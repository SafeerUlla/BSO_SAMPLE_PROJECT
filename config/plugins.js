//module.exports = () => ({});
module.exports = {
    // Other plugins
    'io': {
      enabled: true,
      config: {
        contentTypes: [], // Ensure this is an array, even if empty
        // other configurations
      },
    },
  };
  